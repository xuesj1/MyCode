#include "caryon.h"
#include <bits/stdc++.h>
using namespace std;
using namespace ca; // namespace
int main()
{
    dataname = ""; // dataname
    maxtime = 1000;
    makein(1, 10)
    {
        csh();
        int n = cyrand(1, 1e5);
        in(n);
        instring(" ");
        int d = cyrand(1, 1e5);
        in(d);
        instring("\n");
        for (int i = 0; i < n - 1; i++)
        {
            in(cyrand(0, 1e5));
            instring(" ");
        }
        instring("\n");
        for (int i = 0; i < n; i++)
        {
            in(cyrand(0, 1e5));
            instring(" ");
        }
        // Please finish these rows
    }
    makeout(1, 10);
    debug(1, 10);
    return 0;
}