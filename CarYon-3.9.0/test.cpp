#include "caryon.h"
#include <bits/stdc++.h>
using namespace std;
using namespace ca; // namespace
int main()
{
    dataname = ""; // dataname
    maxtime = 1000;
    makein(1, 10)
    {
        csh();
        int n = cyrand(1, 1e5);
        in(n);
        instring("\n");
        for (int i = 0; i < n; i++)
        {
            in(cyrand(0, 1));
            instring(" ");
        }
        // Please finish these rows
    }
    makeout(1, 10);
    debug(1, 10);
    return 0;
}