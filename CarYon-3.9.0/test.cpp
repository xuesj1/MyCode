#include "caryon.h"
#include <bits/stdc++.h>
using namespace std;
using namespace ca; // namespace
int main()
{
    dataname = ""; // dataname
    maxtime = 1000;
    makein(1, 10)
    {
        csh();
        int w = cyrand(80, 200);
        in(w);
        instring("\n");
        int n = cyrand(1, 3 * 1e4);
        in(n);
        instring("\n");
        for (int i = 0; i < n; i++)
        {
            int pi = cyrand(5, w);
            in(pi);
            instring("\n");
        }
        // Please finish these rows
    }
    makeout(1, 10);
    debug(1, 10);
    return 0;
}