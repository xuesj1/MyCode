#include<bits/stdc++.h>
#define ll long long int
using namespace std;
const int N=(1e4*2);
int u[N],v[N],a[N];
int main()
{
    //xht Orz
    freopen("bus.in","r",stdin);
    freopen("bus.out","w",stdout);
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    ll n,m,k;
    cin>>n>>m>>k;
    for(int i=0;i<m;i++)
    {
        cin>>u[i]>>v[i]>>a[i];
    }
    cout<<-1<<endl;
    return 0;
}