#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int x, g;
    cin >> x >> g;
    if (x >= 10 || g >= 20)
        cout << 1 << endl;
    else
        cout << 0 << endl;
    return 0;
}
